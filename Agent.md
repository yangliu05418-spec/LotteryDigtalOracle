# Agent Development Guidelines

## 项目定位

本项目围绕中国福利彩票双色球历史开奖数据，开展纯数学、统计学、概率论与建模研究。

本项目的讨论、代码、分析、模型和输出均仅用于：

- 组合数学研究
- 概率分布分析
- 随机性检验
- 时间序列与离散事件建模
- 历史数据统计画像
- 模型回测与随机基准比较
- 学术化的数据分析练习

不得将本项目描述为、包装为或用于实际彩票购买建议、投注建议、盈利方案或保证性预测。

## 核心研究原则

1. **纯数学视角**
   - 将双色球视为一个离散组合空间问题。
   - 红球建模为从 `1–33` 中无放回抽取 `6` 个元素。
   - 蓝球建模为从 `1–16` 中抽取 `1` 个元素。
   - 单注组合总数为：

     ```text
     C(33, 6) × 16 = 17,721,088
     ```

2. **统计审慎原则**
   - 不默认历史数据中存在可利用的确定性规律。
   - 不把随机波动误判为因果规律。
   - 所有模型结论必须与随机基准进行比较。
   - 对任何“优势”“偏离”“提升”表述，必须配套统计检验、置信区间或回测证据。

3. **预测表述限制**
   - 可以研究“候选组合生成策略”“概率评分模型”“历史回测表现”。
   - 不得声称模型可以保证预测开奖结果。
   - 不得使用“稳赚”“必中”“必出”“稳赢”等确定性或诱导性语言。
   - 如需表达预测，应限定为数学模型输出或概率性候选集合。

4. **学术目的声明**
   - 本项目只谈论数字、集合、概率、分布、统计检验与模型表现。
   - 不讨论奖金收益、实际购买策略、投注金额或投注行为。
   - 如果输出内容可能被误解为现实投注建议，必须主动加入学术用途说明。

## 双色球规则抽象

### 基本选号空间

- 红球集合：`R = {1, 2, ..., 33}`
- 蓝球集合：`B = {1, 2, ..., 16}`
- 一期开奖号码：

  ```text
  ({r1, r2, r3, r4, r5, r6}, b)
  ```

  其中：

  ```text
  ri ∈ R, ri 互不相同, b ∈ B
  ```

### 建模约定

- 红球视为无序集合，不视为有序序列。
- 蓝球视为单独分类变量。
- 每一期数据默认按独立样本处理，除非正在专门检验时间相关性。
- 原始历史数据应保留期号、日期、6 个红球与 1 个蓝球。

### 常用派生特征

后续分析中，可构造但不限于以下特征：

- 红球频率
- 蓝球频率
- 红球和值
- 红球跨度
- 奇偶比
- 大小比
- 区间分布
- 连号数量
- 重号数量
- 冷热号间隔
- AC 值
- 尾数分布
- 质合比
- 与上一期的交集大小

## 数据与代码规范

1. **数据来源**
   - 使用官方或可信公开数据源。
   - 记录抓取时间、数据范围和数据总量。
   - 不使用脆弱字符串切片解析结构化响应；优先使用 JSON、CSV 或明确格式解析。

2. **数据文件**
   - 默认历史数据文件：

     ```text
     数据/历史数据.csv
     ```

   - 标准字段顺序：

     ```text
     期号, 日期, 红球-1, 红球-2, 红球-3, 红球-4, 红球-5, 红球-6, 蓝球
     ```

3. **可复现性**
   - 所有统计分析应尽量写成可重复运行的脚本或 Notebook。
   - 涉及随机过程时，应支持设置随机种子。
   - 模型评估应记录参数、样本范围和评价指标。

4. **验证优先**
   - 分析脚本应包含基础数据校验：
     - 期号唯一
     - 日期可解析
     - 红球数量为 6
     - 红球范围为 1–33
     - 红球无重复
     - 蓝球范围为 1–16
   - 建模前必须先确认数据质量。

5. **基准优先**
   - 任意模型、策略或评分函数都必须至少与一个纯随机基准比较。
   - 不允许只展示模型结果而不展示随机对照。

## 推荐研究路线

1. 数据质量检查
2. 基础描述统计
3. 理论概率基准计算
4. 随机性与均匀性检验
5. 特征工程
6. 候选组合生成策略
7. 回测框架
8. 与随机策略比较
9. 模型解释与误差分析
10. 结论边界说明

## 输出风格

后续所有分析结论应遵循：

- 先给出数学定义或统计假设。
- 再给出计算方法。
- 再给出结果。
- 最后给出限制与解释。

避免过度结论化。优先使用：

- “在当前历史样本中观察到……”
- “该结果相对于随机基准……”
- “该偏差是否显著需要通过……检验”
- “这不能直接推出未来开奖规律”

避免使用：

- “下一期必然……”
- “一定会出……”
- “稳赚……”
- “必中……”

